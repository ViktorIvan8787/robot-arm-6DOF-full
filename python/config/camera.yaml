# 720p camera settings with first available camera (device 0)
camera:
  device: 0
  width: 1280
  height: 720
  fps: 30
  format: "MJPG"

# Current offset from base unknown (all set to 0)
mount:
  offset: [0.0, 0.0, 0.0]